/**
 * LearnHub Frontend Entry Point
 * Vue 3 + Vite application
 */

import { createApp } from 'vue';
import App from './App.vue';

// Create and mount the Vue app
const app = createApp(App);
app.mount('#app');

console.log('LearnHub app initialized');
