{
  "name": "learnhub-frontend",
  "version": "1.0.0",
  "description": "Vue 3 + Vite frontend for LearnHub lesson booking system",
  "author": "Tony",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist",
    "submit": "node scripts/build-submission.js"
  },
  "dependencies": {
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^5.0.0",
    "gh-pages": "^5.0.0"
  }
}
